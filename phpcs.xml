<?xml version="1.0"?>
<ruleset name="PHP Coding Standard">
    <!-- Default directory to check if there are no directories argument  -->
    <file>app</file>
    <file>routes</file>

    <!-- Add PSR1 standard -->
    <rule ref="PSR1"/>

    <!-- Add PSR2 standard -->
    <rule ref="PSR2"/>

</ruleset>
